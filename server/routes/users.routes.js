module.exports = app => {
    const users = require("../controllers/users.js");

    var router = require("express").Router();

    // Retrieve all Resources
    // router.post("/add", users.add);
   
    // app.use('/npt/resources', router);

}